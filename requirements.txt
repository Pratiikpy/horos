# Horos runtime dependencies.
#
# Pinned to the versions actually installed and tested, not to ranges, because a floating
# dependency silently changes what a paid service returns.

# --- HTTP surface -----------------------------------------------------------------------------
fastapi==0.139.0
uvicorn[standard]==0.40.0
pydantic==2.12.4
httpx==0.28.1

# --- signing and chain ------------------------------------------------------------------------
PyNaCl==1.5.0
# web3 6.x pins hexbytes<0.4 while eth-account 0.13.x needs >=1.2 — the two cannot coexist.
# 7.16.0 is the resolved pair, confirmed by `pip install --dry-run`.
web3==7.16.0
eth-account==0.13.7

# --- market data ------------------------------------------------------------------------------
# ccxt is the data layer for all 105 exchanges; OKX funding-rate and open-interest endpoints
# verified present in repos/ccxt_ccxt/python/ccxt/okx.py.
# Pinned to the version cloned in repos/ccxt_ccxt, so the source read when calibrating limits is
# the source that runs. The 300-row OHLCV cap this codebase paginates around was read from it.
ccxt==4.5.68
pandas==2.2.2
numpy==1.26.4

# --- the model --------------------------------------------------------------------------------
# torch 2.2.2 CPU. Kronos requires >=2.0.0; 2.2.2 is the version already proven to work on this
# machine alongside numpy 1.26.4 (see the clipsai pin matrix in CLAUDE.md).
torch==2.2.2
einops==0.8.1
huggingface_hub==0.33.1
safetensors==0.4.2
tqdm==4.67.1

# --- deterministic metrics --------------------------------------------------------------------
financetoolkit==2.1.4
ta==0.11.0
scikit-learn==1.6.1
scipy==1.15.3
arch==7.2.0
PyPortfolioOpt==1.5.6

# --- tests ------------------------------------------------------------------------------------
pytest==9.0.2
pytest-asyncio==1.3.0
